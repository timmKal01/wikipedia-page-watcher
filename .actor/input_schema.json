{
    "title": "Wikipedia Page Watcher Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "pageTitle": {
            "title": "Page title",
            "type": "string",
            "editor": "textfield",
            "description": "Exact Wikipedia page title, e.g. \"Tesla, Inc.\" or \"Elon Musk\". Must match the page's actual title (check the URL after /wiki/ if unsure)."
        },
        "language": {
            "title": "Language edition",
            "type": "string",
            "editor": "textfield",
            "description": "Wikipedia language edition code, e.g. \"en\", \"de\", \"fr\", \"ja\".",
            "default": "en"
        },
        "daysBack": {
            "title": "Days back",
            "type": "integer",
            "editor": "number",
            "description": "How many days back from today to search, by edit timestamp.",
            "default": 30,
            "minimum": 1,
            "maximum": 365
        },
        "maxResults": {
            "title": "Max results",
            "type": "integer",
            "editor": "number",
            "description": "Maximum number of edits to return, most recent first.",
            "default": 25,
            "minimum": 1,
            "maximum": 100
        }
    },
    "required": ["pageTitle"]
}
